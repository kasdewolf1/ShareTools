<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/handlebars/dist/handlebars.min.js"></script> 
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/toolinfo.css">
  <link rel="stylesheet" href="/header.css">
  <link rel="icon" type="image/x-icon" href="/img/favicon-32x32.png">
    <title>Share Tools</title>
</head>

<body>
<div class="page-container">
  {{> header}}

  <div class="profiel-container">
    <a href="/indexloggedin" class="btn-terug">🞀</a>
    <h1>{{product.title}}</h1>

    <section class="details-container">
      {{#if product.imageURL}}
        <img src="{{product.imageURL}}" alt="Tool Image">
      {{else}}
        <img src="/img/Tool Filler.png" class="toolfoto">
      {{/if}}


      <section class="tekst-info">            
          <p class="info-label">Afmeting</p>     
            {{#if product.afmeting}}         
              <p class="info">{{product.afmeting}}</p>
            {{else}}
              <p>Niet van toepassing / Niet ingevuld</p>
            {{/if}}


          <p class="info-label">Categorie</p>              
            {{#if product.categorie}}
              <p class="">{{product.categorie}}</p>
            {{else}}
              <p>Niet van toepassing / Niet ingevuld</p>
            {{/if}}


          <p class="info-label">Locatie</p>
            {{#if product.locatie}}
              <p class="">{{product.locatie}}</p>
            {{else}}
              <p>Niet van toepassing / Niet ingevuld</p>
            {{/if}}
            

          <p class="info-label">Status</p>
            {{#if product.status}}
              <p class="status">{{product.status}}</p></li>
            {{else}}
              <p class="status">Niet van toepassing / Niet ingevuld</p>
            {{/if}}

            <br>
            <br>
            <p>{{product.beschrijving}}</p>
      </section>
    </section>      

    <div class="buttons">
      <button class="delete-btn" onclick="deleteProduct('{{id}}')">
        <i class="fas fa-trash-alt"></i>
      <button type="button" class="btn-dark">LEEN</button>
      <button type="button" class="btn-dark">BERICHT</button>
    </div>
  </div>
</div>

<script src="/script.js"></script>
</body>
</html>