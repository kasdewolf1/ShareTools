<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Productdetails</title>
  <link rel="stylesheet" href="/toolinfo.css">
  <!-- Voeg Handlebars.js toe -->
  <script src="https://cdn.jsdelivr.net/npm/handlebars/dist/handlebars.min.js"></script>
  
</head>
<body>
 <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet"> <link rel="stylesheet" href="/toolinfo.css"> <link rel="stylesheet" href="/header.css"> <link rel="icon" type="image/x-icon" href="/img/favicon-32x32.png"> <title>Share Tools</title> </head> <body>
  
<div class="page-container">
  {{> header}}
<div class="profiel-container">

<button class="btn-terug">🞀</button>
<h1>{{title}}</h1>

<section class="details-container">
  {{#if imageURL}}
    <img src="{{imageURL}}" alt="Tool Image" class="tool-image">
  {{else}}
    <img src="/img/Tool Filler.png" class="tool-image">
  {{/if}}


<section class="tekst-info">            
    <p class="info-label">Afmeting</p>     
    {{#if afmeting}}         
        <p class="info">{{afmeting}}</p>
    {{else}}
        <p>Niet van toepassing / Niet ingevuld</p>
    {{/if}}

    <p class="info-label">Categorie</p>              
    {{#if categorie}}
        <p class="">{{categorie}}</p>
    {{else}}
        <p>Niet van toepassing / Niet ingevuld</p>
    {{/if}}

    <p class="info-label">Locatie</p>
    {{#if locatie}}
        <p class="">{{locatie}}</p>
    {{else}}
        <p>Niet van toepassing / Niet ingevuld</p>
    {{/if}}

    <p class="info-label">Status</p>
    {{#if beschikbaarheid}}
        <p class="status">{{beschikbaarheid}}</p>
    {{else}}
        <p class="status">Niet van toepassing / Niet ingevuld</p>
    {{/if}}

    <br>
    <br>
    <p>{{beschrijving}}</p>
</section>
</section>      

<div class="buttons">
  <button class="delete-btn" onclick="deleteProduct('{{id}}')">
    <i class="fas fa-trash-alt"></i>
  <button type="button" class="btn-dark">LEEN</button>
  <button type="button" class="btn-dark">BERICHT</button>
</div>
</div>  
</div>
<!-- Inside your productinfo.ejs or productinfo.pug or whatever template engine you are using -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Get the product ID from localStorage
    const productId = localStorage.getItem('productId');
    if (productId) {
        // Remove the productId from localStorage to prevent repeated fetches
        localStorage.removeItem('productId');
        // Reload the page with the productId as a query parameter
        window.location.href = `/productinfo?productId=${productId}`;
    }
});
</script>

</body>
</html>